# 🍳 夫妻共享食譜 App

一個專為夫妻設計的私人食譜分享平台，讓兩人能夠輕鬆記錄、分享和管理家庭料理。

## ✨ 功能特色

### 📸 豐富的視覺體驗
- **多圖片支援**: 每道食譜可上傳最多 5 張照片
- **圖片輪播**: 在食譜詳情頁左右滑動瀏覽照片
- **觸控優化**: 支援手機觸控滑動手勢

### 📝 完整的食譜管理
- **食譜資訊**: 菜名、食材清單、烹飪步驟、份量、烹飪時間
- **分類系統**: 主食、湯品、小菜、甜點、飲品
- **來源連結**: 支援多個食譜來源網址（YouTube 影片、部落格等）

### 🔧 便利的操作功能
- **搜尋功能**: 快速搜尋食譜
- **分類篩選**: 依類別篩選食譜
- **新增/編輯/刪除**: 完整的 CRUD 操作
- **食材勾選**: 烹飪時可勾選已準備的食材

## 🛠 技術架構

### 前端
- **框架**: React 18 + TypeScript
- **建置工具**: Vite
- **路由**: Wouter
- **狀態管理**: TanStack React Query
- **UI 元件**: shadcn/ui (Radix UI + Tailwind CSS)
- **檔案上傳**: Uppy v5

### 後端
- **執行環境**: Node.js
- **框架**: Express.js
- **語言**: TypeScript (ESM)

### 資料儲存
- **資料庫**: PostgreSQL
- **ORM**: Drizzle ORM
- **驗證**: Zod
- **檔案儲存**: Replit Object Storage (Google Cloud Storage)

## 📁 專案結構

```
├── client/                 # React 前端
│   └── src/
│       ├── components/     # UI 元件
│       │   ├── ui/         # shadcn/ui 基礎元件
│       │   ├── recipe-card.tsx
│       │   └── recipe-detail.tsx
│       ├── pages/          # 頁面元件
│       │   ├── home.tsx
│       │   ├── add-recipe.tsx
│       │   └── edit-recipe.tsx
│       ├── hooks/          # 自訂 Hooks
│       └── lib/            # 工具函式
├── server/                 # Express 後端
│   ├── index.ts            # 進入點
│   ├── routes.ts           # API 路由
│   ├── storage.ts          # 資料存取層
│   ├── db.ts               # 資料庫連接
│   └── replit_integrations/
│       └── object_storage.ts
├── shared/                 # 共用程式碼
│   └── schema.ts           # 資料模型定義
└── design_guidelines.md    # 設計規範
```

## 🗃 資料模型

### Recipe (食譜)
| 欄位 | 類型 | 說明 |
|------|------|------|
| id | varchar | 主鍵 (UUID) |
| name | text | 菜名 |
| imageUrls | text[] | 圖片路徑陣列 (最多5張) |
| ingredients | text[] | 食材清單 |
| steps | text[] | 烹飪步驟 |
| servings | integer | 份量 (人數) |
| cookTime | text | 烹飪時間 |
| category | text | 分類 |
| sourceUrls | text[] | 來源網址陣列 |

## 🚀 API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| GET | `/api/recipes` | 取得所有食譜 |
| GET | `/api/recipes/:id` | 取得單一食譜 |
| POST | `/api/recipes` | 新增食譜 |
| PATCH | `/api/recipes/:id` | 更新食譜 |
| DELETE | `/api/recipes/:id` | 刪除食譜 |
| POST | `/api/uploads/request-url` | 取得圖片上傳 URL |
| GET | `/objects/:objectPath` | 取得上傳的檔案 |

## 🎨 設計特色

- **手機優先**: 響應式設計，優先考慮行動裝置體驗
- **繁體中文**: 完整的繁體中文介面
- **溫暖配色**: 橘色為主的親密溫暖色調
- **字體選擇**: Playfair Display (標題) + Inter (內文)
- **觸控友善**: 足夠大的按鈕和觸控區域

## 📦 安裝與執行

### 環境需求
- Node.js 20+
- PostgreSQL 資料庫

### 安裝步驟

1. 複製專案
```bash
git clone <repository-url>
cd couples-recipe-app
```

2. 安裝依賴
```bash
npm install
```

3. 設定環境變數
```bash
# .env 檔案
DATABASE_URL=postgresql://...
```

4. 初始化資料庫
```bash
npm run db:push
```

5. 啟動開發伺服器
```bash
npm run dev
```

應用程式將在 `http://localhost:5000` 啟動。

## 📄 授權

MIT License

## 👥 貢獻者

本專案由夫妻共同設計與開發。
